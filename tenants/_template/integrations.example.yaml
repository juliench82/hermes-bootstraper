# Example connector metadata only. Never add tokens or credentials here.
tenant_id: example-tenant
integrations:
  - name: accounting-example
    purpose: read open invoices for a weekly summary
    operations:
      - read
    data_scope:
      - open-invoices
    credential_ref: secret-store://example-tenant/accounting-example
    status: requested
    owner: operator@example.invalid
